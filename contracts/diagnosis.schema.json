{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "diagnosis/v1",
  "type": "object",
  "required": ["schema_version", "run_id", "metrics", "diagnostics", "report_ready"],
  "properties": {
    "schema_version": {"const": "diagnosis/v1"},
    "run_id": {"type": "string"},
    "metrics": {"type": "object"},
    "diagnostics": {"type": "object"},
    "report_ready": {"type": "boolean"}
  }
}
